{
  "name": "tp_express",
  "version": "1.0.0",
  "description": "TP cours NodeJS",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": " nodemon index.js",
    "dev": "browser-sync start --proxy 'localhost:3000' --files '**/*.{js,css,html}' --port 3001 --reload-delay 500 --no-notify"
  },
  "author": "Maxime Bidan",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "browser-sync": "^3.0.2",
    "ejs": "^3.1.10",
    "express": "^4.19.2",
    "express-session": "^1.18.0",
    "mongoose": "^8.4.0",
    "nodemon": "^3.1.0",
    "serve-favicon": "^2.5.0"
  },
  "nodemonConfig": {
    "watch": [
      "index.js",
      "views/**/*.html",
      "public/**/*.css"
    ]
  }
}
